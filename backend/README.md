# Backend

This folder is reserved for your backend code.

Right now the project only contains a frontend app in `../frontend`.

If you add a backend later, put its server files here and update the scripts in this folder as needed.
